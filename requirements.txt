# Core data processing
numpy>=1.24.0
pandas>=1.5.3

# Deep learning framework
torch>=2.0.0

# Model training and evaluation
optuna>=3.0.0
plotly>=5.0.0
scikit-learn>=1.2.0

# NVIDIA GPU support requires a CUDA-enabled PyTorch wheel. Select the command
# matching the installed NVIDIA driver at https://pytorch.org/get-started/locally/
# before installing the remaining dependencies from this file.
