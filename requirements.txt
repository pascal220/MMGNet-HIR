# Core Data Processing
numpy>=1.21.0
pandas>=1.3.0

# Deep Learning Framework
# For Windows CPU-only installation (recommended for cross-platform compatibility)
torch>=1.10.0
torchvision>=0.11.0

# Optional: For GPU support on Windows, install CUDA-enabled PyTorch separately:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
